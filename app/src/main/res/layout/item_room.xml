<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#B6C5CC">

    <!-- Room Image -->
    <ImageView
        android:id="@+id/roomImage"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:src="@drawable/TherapyRoom"
        android:contentDescription="Room Image" />

    <!-- Room ID -->
    <TextView
        android:id="@+id/roomId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Room ID: #12345"
        android:textColor="#000000"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp" />

    <!-- Occupancy Rate -->
    <TextView
        android:id="@+id/occupancy"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Occupancy Rate: 75%"
        android:textColor="#333333"
        android:textSize="16sp"
        android:layout_marginTop="8dp" />

    <!-- Room Entry Button -->
    <Button
        android:id="@+id/roomButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Enter Room"
        android:textColor="#FFFFFF"
        android:backgroundTint="@color/colorPrimary"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/backToMainPageButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back to Main Page"
        android:textColor="#FFFFFF"
        android:backgroundTint="@color/colorPrimary"
        android:layout_marginTop="16dp" />

</LinearLayout>
